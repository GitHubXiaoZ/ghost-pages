/*inital state*/
const initialState = {
    post: [],
    post_list: {},
    loading: false
}

/*exports post reducer*/
export default function(state = initialState, action) {
    switch (action.type) {
        default: 
            return state 
    }
}
